# PR #35 review — te-B2: idc-adapter-pi (coms-net resident mapping)

- **Issue:** #29 (unit B2) · **Run:** pi-unification-plan-2026-06-13-0352
- **PR head reviewed:** `2a66621` (in a detached scratch worktree, independent of the writer's checkout)
- **Slice type:** docs (TDD Red→Green EXEMPT — structural smoke + content ship together)
- **Reviewer:** pr-reviewer (adversarial, no source edits)

## VERDICT: REQUEST-CHANGES — 0 Blocker · 1 Major · 0 Minor · 0 Nit

The adapter content is correct, complete, and cross-unit consistent with the merged A2 and B1
contracts. **It does not merge as-is: it turns CI red.** One Major must be fixed first; it is a
trivial 3-token namespace correction with no content/design change.

---

## MAJOR-1 — PR introduces 3 CI-breaking reference-lint violations (green → red regression)

`bash scripts/lint-references.sh` is **CLEAN on the integration base** (32 files scanned) and
**FAILS at the PR head** with three new Rule-G `[bare-component-ref]` findings, all in the new
`skills/idc-adapter-pi/SKILL.md`:

| Line | Written as | Must be |
|---|---|---|
| 51 | `` `idc-implementer.md` `` | `idc:idc-implementer` |
| 57 | `` `idc-finisher.md` `` | `idc:idc-finisher` |
| 78 | `` `idc-adapter-claude` `` | `idc:idc-adapter-claude` |

- All three tokens resolve to **real components** (`agents/idc-implementer.md`,
  `agents/idc-finisher.md`, `skills/idc-adapter-claude/`) → valid findings, not false positives.
- The merged sibling docs consistently namespace these exact refs
  (`agents/idc-build.md`, `agents/idc-finisher.md` use `idc:idc-implementer` / `idc:idc-finisher`
  / `idc:idc-adapter-claude`). The pi SKILL.md is the lone deviation.
- **`lint-references.sh` is wired into CI** (`.github/workflows/ci.yml:21`), so these violations
  red-light the merge gate. The lead explicitly required this gate clean (review brief, step 6).
- **Fix:** rewrite the three backticked refs to the `idc:idc-…` namespaced form (matching the
  convention in `idc-build.md`/`idc-finisher.md`). No structural change; re-run
  `bash scripts/lint-references.sh` → must print `CLEAN`.

Severity = Major (not Blocker): a CI-breaking, PR-attributable regression on a required gate, but
the adapter's substance is otherwise sound and the fix is a mechanical 3-line edit.

---

## Everything else verified — PASS

**Smoke genuineness (Y).** `tests/smoke/phase8-adapter-pi.sh` passes at PR head and is a genuine
structural assertion, not trivial grep theatre: it (a) requires the three bold primitive labels
(`**durable worker**`, `**bounded fan-out**`, `**goal loop**`) in **all three** adapter files
(asserts the parallel runtime-blind interface), (b) asserts pi's concrete targets
(resident / ephemeral-child-process / `/fullauto-goal`), and (c) asserts the worked example +
named merge mechanism with its specific components (`board-backed`, `lease`, `matrix-disjoint`,
`no master orchestrator`), plus single-source / glass-wall / downstream / fail-closed.

**Mirrors the other two adapters (Y).** Exact interface shape: same frontmatter (description names
the three primitives), same `## Primitive → mechanic map` heading, same three-column
`Primitive | mechanic | Fallback` table with the same three bold labels, same
`### Durable-worker rules (load-bearing)` + `## Authority boundaries`. Adds the issue-required
`### The Build triplet as residents — worked example` and a `## Model selection` that correctly
mirrors `idc-adapter-claude`'s tier-resolution (not Codex's untiered carve-out) — right call,
since pi residents run the Pi agent under the tier table.

**Three primitives mapped the pi way (Y).** durable worker → standing coms-net resident (a pool of
build triplets, `--name <role>` peers, no master orchestrator); bounded fan-out → ephemeral
coms-net helper / isolated child-process (review fan-out = fresh cold child-processes);
goal loop → `/fullauto-goal`. All three match the brief exactly.

**Merge-serialization matches A2 (Y).** Near-verbatim with the merged A2 contract:
`agents/idc-finisher.md` §"Merge serialization (load-bearing — the A2↔B2 contract)" (L60–80) and
`agents/idc-build.md` (L56–65). Both define the identical two layers — (1) matrix-disjoint
surfaces make diffs content-commutative; (2) single-holder merge lease, fail-closed (no lease →
no merge) — and the **pi row**: flat pool / no master orchestrator → the authoritative GitHub
Projects board IS the lock-holder; the finisher resident acquires the board-backed lease, merges
only the integration-ref, releases; coms-net carries only liveness/notification. No contradiction.

**Glass-wall ACL honored + matches B1 (Y).** The SKILL.md river order
`think → plan → sequence → build-impl → build-review → build-finish` + Ripple universal downstream
sink, fail-closed on upstream/self/unknown, is an **exact match** to B1's vendored
`RIVER_ORDER` constant (`runtime/pi/extensions/idc-role-harness.ts:180`) and its
`evaluateComsNetSendForRole` semantics (Ripple sink always allowed; ripple-as-source denied;
downstream-only). The forward triplet notifications `build-impl → build-review → build-finish`
are correctly noted as downstream-legal.

**Single-source (Y).** States repeatedly that playbooks are not forked — each stage resident runs
its **whole** playbook; the adapter decides only how *sessions* are realized. Describes mapping
only; does **not** duplicate/fork the vendored runtime code (residents launched by B1's
`idc-pi` launcher).

**Layers = service (PASS).** Changed paths: `skills/idc-adapter-pi/SKILL.md` (new),
`tests/smoke/phase8-adapter-pi.sh` (new), `tests/smoke/run-all.sh` (+1 line registration).
2 net-new authored files — well under the >4 deep-module threshold. Adapter skill = service layer.

**Project-protocol (PASS except MAJOR-1).** No `no_edit_paths` touched
(`docs/dev/2026-06-13-pi-unification-plan.md`, `LICENSE` untouched). Did not edit the other
adapters' files, Track A files, or vendored runtime internals. `bash tests/smoke/run-all.sh` is
green (14/14). The PR's base-vs-integration conflict on `tests/smoke/run-all.sh` is the expected
shared-append surface the lead deconflicts separately — **not** treated as a finding.

---

## Reproduce

```
git worktree add --detach .claude/worktrees/rev-pr35 2a66621
cd .claude/worktrees/rev-pr35
bash scripts/lint-references.sh        # FAILS: 3 bare-component-ref (L51/L57/L78)
bash tests/smoke/phase8-adapter-pi.sh  # PASS
bash tests/smoke/run-all.sh            # ALL GREEN (14/14)
# base for comparison:
cd /Users/jeremy/dev/proj/idc-workflow && bash scripts/lint-references.sh   # CLEAN (32 files)
```
