# Re-verdict: PR #148 then PR #147 round-2 deltas
Scope: delta-only re-verdict against prior reports:
- PR #148 U1 `072aa0832b4761e590b7eec663d219f307360655` -> `686df2f769ad12e78996dd798b1e779e41cd52ed`
- PR #147 U3/U4 `66223088ef66f5c20e740d8b47f3c7a6d578b5df` -> `3f60d4b99f02fff02c8f0975331fffea93089295`

## PR #148 / U1 journal-spine
Verdict: PASS

Prior finding dispositions:
- [BLOCKER] Journal append failures are swallowed after mutation: DESK-WAIVED per operator re-verdict packet (fail-soft journal per plan §6 with U2 reconciliation as detector); code remains fail-soft by design.
- [BLOCKER] Same-status retry denial regressed `engine-readback-verify.sh`: FIXED. Delta changes same-status transition to an idempotent no-op; `bash tests/smoke/governance/engine-readback-verify.sh` passes at `686df2f`.
- [MAJOR] Link journal enforcement unpinned: FIXED. `journal-append.sh` now covers fs and github create/link/move/close; deleting the link journal append line makes the test fail (`expected 5 journal lines, got 4`).

Verification run:
```bash
cd /tmp/pi-harnesses/review/pr148-reverdict
bash scripts/lint-references.sh
bash tests/smoke/governance/journal-append.sh
bash tests/smoke/governance/journal-append-only.sh
bash tests/smoke/governance/engine-readback-verify.sh
```
Observed: all pass.

## PR #147 / combined U3+U4
Verdict: PASS

Prior finding dispositions:
- [BLOCKER] U3 cross-check did not validate transition/op references: FIXED. Delta validates low-noise `eng <op>` references against `ops:`; seeded `eng teleport-ticket` is caught while valid `eng move` is not flagged.
- [MAJOR] File-surface trespass: DESK-WAIVED per operator re-verdict packet; surface expansion granted.
- [NIT] PR body only auto-closes #143: FIXED. PR body now includes `Closes #143` and `Closes #144`.

Verification run:
```bash
cd /tmp/pi-harnesses/review/pr147-reverdict
bash scripts/lint-references.sh
bash tests/smoke/governance/machine-yaml-crosscheck.sh
bash tests/smoke/governance/release-gate-governance.sh
python3 scripts/idc_release_check.py --governance
```
Observed: all pass; full governance lane reports `ALL GREEN (38 real scenario(s) + self-check)`.

Op-ref probe:
```bash
printf '\nInvalid op: eng teleport-ticket\nValid op: eng move --num 1 --to-status Todo\n' >> commands/doctor.md
bash scripts/lint-references.sh
```
Observed: fails on `Invalid transition engine op: 'eng teleport-ticket'` and does not flag `eng move`.
