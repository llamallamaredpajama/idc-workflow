# PR #32 Review — te-A2: canonical impl→review→finish triplet (new idc-finisher)

**Verdict: PASS-WITH-NITS** — content + structural smoke ship together (docs slice, TDD-exempt); all substance requirements met; no Blocker/Major. One Minor (test-coverage guard gap) + one Nit (PR prose).

- **PR:** #32 · **Issue:** #25 (unit A2) · **Head:** `0b148cd`
- **Run:** pi-unification-plan-2026-06-13-0352 · **Integration:** `te-integration/pi-unification-plan-2026-06-13-0352`
- **Slice:** docs (TDD Red→Green EXEMPT) · **Layers claimed:** service, cli
- **Reviewed independently** in a detached scratch worktree at PR head (now removed). Not trusting the writer's self-report.

> The known `tests/smoke/run-all.sh` merge-conflict with the integration branch is the trivial shared-append surface (lead deconflicts separately) — **not** treated as a finding here.

---

## Verification (run at PR head, isolated worktree)

| Check | Result |
|---|---|
| `bash tests/smoke/phase4-triplet.sh` (unit) | **PASS** (exit 0) |
| `bash tests/smoke/run-all.sh` (full) | **ALL GREEN** — 10 phases incl. `phase4-triplet` |
| `bash scripts/lint-references.sh` | **CLEAN** (exit 0, 31 files scanned) |
| Files changed vs merge-base | 6 files, +222/−46. Net-new authored: **2** (`agents/idc-finisher.md`, `tests/smoke/phase4-triplet.sh`) — under the >4 deep-module bar |
| Off-limits surfaces (A1 engine/skill, A3 tracker/Stage, Track B adapters, `phase4-build.sh`) | **untouched** |
| `no_edit_paths` (`docs/dev/2026-06-13-pi-unification-plan.md`, `LICENSE`) | **untouched** |

---

## Done-When (issue #25) — met

The smoke is **genuine**, not a trivial-string grep. It asserts the real, load-bearing contract:

- **(a) `idc-finisher.md`** — exists; names `/simplify`, `git finaliz`ation, `merge`, `tidy`, its OWN `/fullauto-goal` loop, the Ripple-on-unsolvable, **and the complete 6-element posture** (it greps the literal `6-element` token *plus each of the six named elements*: outcome, verification surface, constraints, boundaries, iteration policy, blocked-stop). Asserting all six names — not just one — is what makes this a contract assertion rather than a trivial check.
- **(b) `idc-build.md`** — all three roles (implementer / reviewer-agent / finisher), the per-runtime session mapping (`resident` / `teammate` / `thread`), the fallback-collapse rule (`collapse` + `fallback`), and the merge-serialization mechanism (`serializ` + `matrix-disjoint|disjoint` + `lock|queue`).

`set -uo pipefail` with every `has` guarded by `|| fail` — no silent-pass path.

---

## Substance (the load-bearing A2↔B2 sync source) — verified by reading both files

**Merge-serialization mechanism — EXPLICIT and CONSISTENT in BOTH files:**

- `idc-finisher.md` carries a dedicated **"Merge serialization (load-bearing — the A2↔B2 contract)"** section: two required layers — (1) matrix-disjoint surfaces (content-commutative diffs); (2) a **single-holder merge lease, fail-closed** (no lease → no merge). Realizations: **pi** = board-backed merge lease (authoritative board is lock-holder, no master orchestrator); **Claude Teams / collapsed fallback** = single Build orchestrator as sole merger; **Codex** = app-server's serial merge of finisher threads. "One mechanism … realization is adapter-decided."
- `idc-build.md` Phase 3 states the identical mechanism and the identical three realizations. The two files agree — B2 (#29) can consume the pi row unambiguously.

**Role contract — correct:**

- `idc-implementer.md` is now **engine-only**: claim → `/fullauto-goal` loop → **hand off to review**. Step 4 + Authority explicitly: *"the implementer does not fix review findings or merge"* / *"never applies review fixes and never merges (the finisher does both)."* (Failing-test-first discipline retained.)
- `idc-finisher.md` **owns fix-application + merge**: *"the only role that applies review fixes and merges"* — own `/fullauto-goal` fix loop over **all** findings incl. side issues → `/simplify` (Claude native; adapter maps/skips for Codex) → acquire lease → merge → tidy → Ripple-on-unsolvable.
- `idc-build.md` / `commands/build.md` stay **single-source**: explicitly *"single-source (no per-runtime forks); the adapter decides only how their sessions are realized."* No per-runtime playbook forks. Fallback-collapse rule present (last-resort only; never the Codex default).

**Layers:** service ↔ `agents/*.md` (build/finisher/implementer); cli ↔ `commands/build.md`. Both map to changed paths. ✓

**Forward-ref / lint hygiene:** `idc:idc-review-engine` (skill) and `idc:idc-review-coordinator` (agent) are namespaced **and valid** — A1 (#30) already merged, so these resolve and are correct backward refs, not dangling. The only genuine forward-ref — B2's pi adapter — is kept **descriptive** ("the pi runtime adapter", never `idc:idc-adapter-pi`), so Rule A stays clean now and after B2 merges. Reasonable and lint-clean.

---

## Findings

### Minor

**M1 — Smoke does not guard the merge-serialization section *inside `idc-finisher.md`*.**
`idc-finisher.md` is the explicit A2↔B2 sync source B2 (#29) reads, and it *does* ship a full "Merge serialization" section (verified). But `phase4-triplet.sh` asserts the merge-serialization terms (`serializ` / `disjoint` / `lock|queue`) only against `idc-build.md` — not against `idc-finisher.md`. A future edit could strip the section from `idc-finisher.md` and the smoke would stay green, silently breaking the contract downstream B2 consumes. This is **in-spec** (issue #25 Done-When only requires the build.md assertion), so it is not a Done-When miss — but mirroring ~3 greps (`serializ`, `matrix-disjoint|disjoint`, `lease|lock`) against `$FIN` would close the regression gap cheaply. *Optional; non-blocking.*

### Nit

**N1 — PR-body prose slightly conflates the two forward-ref cases.** The body says *"Forward-refs to A1's review-agent **and** B2's adapter-pi are kept descriptive (not namespaced)."* In fact A1's review-agent IS namespaced (`idc:idc-review-engine` / `idc:idc-review-coordinator`) and valid because A1 already merged; only B2's pi adapter is the genuine descriptive forward-ref. Implementation is correct and lint-clean — only the prose is imprecise. Cosmetic.

---

## Bottom line

Content and structural smoke shipped together per the docs-slice rule. The triplet role split (engine-only implementer / fix+merge-owning finisher), the single-source build playbook, the fallback-collapse rule, and — most importantly — the merge-serialization mechanism are all present, explicit, and **consistent across `idc-finisher.md` and `idc-build.md`**, so the A2↔B2 sync point is unambiguous for B2 (#29). Smoke is genuine; full suite + lint green; no off-limits or no_edit_path touched; 2 net-new authored files. **PASS-WITH-NITS** — M1/N1 are non-blocking and may be deferred.
