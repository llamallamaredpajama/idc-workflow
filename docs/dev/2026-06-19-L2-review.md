# Code Review — commit 1014a1e (L2-1 friction / L2-2 nit)

**Scope:** `5646a59..1014a1e` (1 commit, 5 files, +52 / −7 lines)
**Verdict:** PASS-WITH-NITS
**Risk tier:** lite
**Packet:** `.code-review/runs/1014a1e-20260619-064132`
**Reviewer completion:** inline coverage of all 13 dimensions (prose+test-only commit; no executable code paths to fan out to subagents)
**Mode:** read-only (Dimension 13: Not requested — default read-only review)

Commit subject: `fix(idc-autorun): exit report reconciles the working tree post-build (L2-1) + retire Stage won't-fix (L2-2)`

Files: `agents/idc-autorun.md`, `commands/autorun.md`, `docs/dev/L1-remediation-plan.md`, `skills/idc-tracker-github/SKILL.md`, `tests/smoke/phase6-autorun.sh`. All are instruction markdown or a prose-grep shell test — no runtime executable code changed.

## 13-dimension coverage

| # | Dimension | Status | Note |
|---|---|---|---|
| 1 | Repo Protocol | PASS | `idc:<name>` namespacing intact; `${CLAUDE_PLUGIN_ROOT}` used markdown-only; no personal paths; `lint-references.sh` exit 0 (33 files). |
| 2 | Schema & Contract Drift | PASS | No field/enum change. L2-2 doc accurately describes the existing `Status/Stage` enums + `(.stage // "Buildable")` query default. |
| 3 | Error Handling Integrity | N/A | No error-handling code touched. |
| 4 | Resource Management | N/A | No resources. |
| 5 | Security | PASS | No security-sensitive files; no secrets; no URLs introduced. |
| 6 | Stack Gotcha Audit | PASS | `grep -qiE` patterns are valid POSIX ERE; `set -uo pipefail` test harness unaffected. |
| 7 | Unit-Test Rigor | PASS-WITH-NITS | L2-1 lock is genuinely red-when-broken (verified). One coverage-asymmetry nit (below). |
| 8 | Integration-Test Gap | PASS | Behavioral lock is a prose-grep at the correct altitude for instruction markdown. |
| 9 | Dependency & Bloat | PASS | No dependency change; doc/test only. |
| 10 | Complexity Budget | PASS | +9 test lines, two tight greps; minimal. |
| 11 | Git-History Narrative | PASS | Subject + body accurately describe the diff; both items labelled and scoped. |
| 12 | Stale-Docs Sweep | PASS-WITH-NITS | One dangling doc pointer (below). |
| 13 | Simplification Applied | — | Not requested — default read-only review. |

## Reviewer results

| Reviewer | Required | Status | Verdict | Findings |
|---|---|---|---|---:|
| protocol (inline) | yes | accepted | pass | 0 |
| unit-test (inline) | yes | accepted | pass-with-nits | 1 |
| stale-docs (inline) | yes | accepted | pass-with-nits | 1 |
| schema/contract (inline) | yes | accepted | pass | 0 |
| dependency (script→inline fallback) | yes | accepted | pass | 0 |
| git-narrative (script→inline fallback) | yes | accepted | pass | 0 |

Dimension-9/11 helper scripts were not present under the skill base dir; performed the equivalent read-only git inspection inline (recorded as fallback).

## Verification performed (read-only)

- `bash scripts/lint-references.sh` → exit 0, "CLEAN (33 files scanned)".
- `bash tests/smoke/phase6-autorun.sh` → PASS (exit 0) against committed 1014a1e state (uncommitted working-tree edits to `SKILL.md`/`L1-remediation-plan.md` stashed first, then restored — those edits are out of scope for this single-commit review).
- **L2-1 red-when-broken proof:** reverted `agents/idc-autorun.md` to its parent (5646a59) state and re-ran phase6 → `FAIL: idc-autorun.md exit report must source its working-tree state from a post-build git status …` (exit 1). The lock is NOT a tautology.
- **L2-2 factual validation:** traced both runtime consumers. `scripts/idc_autorun_drain.py:64` skips any non-`Todo` item before Stage is examined; `agents/idc-build.md:17-18` selects `Status=Todo`. A retired pointer is `Status=Done` + closed, so neither consumer acts on it regardless of Stage — the "no consumer acts on a Done+Planning item" claim holds.

## Blocker

_None._

## Major

_None._

## Minor

**M1 — `commands/autorun.md` exit-report fix is not locked by any smoke test (coverage asymmetry).** `tests/smoke/phase6-autorun.sh:17` pins `AUTORUN="$PLUGIN/agents/idc-autorun.md"`; the two new L2-1 greps (lines 67, 69) check only that file. `commands/autorun.md` received the equivalent post-build fix but no test guards it, so it could silently regress (lose the post-build instruction) while the suite stays green. Mitigating fact that keeps this Minor, not Major: `commands/autorun.md:6-8` tells the runtime to "read `${CLAUDE_PLUGIN_ROOT}/agents/idc-autorun.md` end-to-end, then run its two-lane drain loop" — the agent file is the authoritative behavioral source, and it IS locked. The command file's prose is duplicative documentation, so a regression there would not change runtime behavior. Optional unblock: add the same two greps against `commands/autorun.md`, or have the test assert both files. (confidence 0.97)

## Nit

**N1 — dangling doc pointer `docs/dev/L2-findings.md`.** `docs/dev/L1-remediation-plan.md:103` and `:109` (added by this commit) cite `docs/dev/L2-findings.md` ("See L2-2 below" and the `## Loop 2 (L2-discovery) — `docs/dev/L2-findings.md`" heading), but that file does not exist in the 1014a1e tree (`ls docs/dev/L2-findings.md` → No such file). `lint-references.sh` does not catch it: its dangling-path scan covers only `agents skills commands templates` (line 34) and explicitly allow-lists `docs/dev/` (line 39), so `docs/dev/` cross-references are unguarded by tooling. Impact is contained — `docs/dev/` is not shipped and does not affect plugin behavior; it is an internal dev-note broken pointer. Unblock: create the referenced `L2-findings.md`, or change the citation to an inline reference (the L2-2 content is already fully captured in-place in `L1-remediation-plan.md` and `SKILL.md`, so the pointer is arguably redundant). (confidence 0.95)

## Observations (not findings)

- **L2-2 footgun is defense-in-depth, not a live bug.** The "clearing Stage reads as Buildable via `(.stage // "Buildable")`" footgun only bites a consumer that queries `--stage Buildable` *without* also pinning `Status=Todo`. Both real consumers (drain script, Build) DO pin `Status=Todo` first, so for a `Status=Done` retired pointer the footgun is theoretical. The commit's conclusion (leave Stage as-is) is nonetheless correct and harmless; the reasoning is sound as a defense-in-depth argument. No action needed.
- **Out-of-scope working-tree edits exist.** `skills/idc-tracker-github/SKILL.md` and `docs/dev/L1-remediation-plan.md` carry uncommitted edits that further expand the L2-2 rationale (adding the filesystem-backend `setField Stage` enum-rejection argument). These are not part of commit 1014a1e and were excluded from this review; they strengthen — not contradict — the committed reasoning.

## Surfaces cleared

- `agents/idc-autorun.md` exit-report bullet — post-build `git status --porcelain` instruction present, locked red-when-broken by phase6.
- `scripts/idc_autorun_drain.py` / `agents/idc-build.md` — confirmed both gate on `Status=Todo` before Stage, validating the L2-2 "no Done+Planning consumer" claim.
- `lint-references.sh` — exit 0; `phase6-autorun.sh` — exit 0 on committed state.
- Namespacing, `${CLAUDE_PLUGIN_ROOT}` usage, no-personal-paths conventions — all intact.
