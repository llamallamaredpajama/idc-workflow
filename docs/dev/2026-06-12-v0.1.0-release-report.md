# v0.1.0 Release Report — Phase E gauntlet

- **Date:** 2026-06-11 → 2026-06-12
- **Executor:** phase-e (Fable 5), idc-migration team
- **Status at writing:** all local stages complete; repo delete+recreate pending operator unblock; flip + tag + issues pending fidelity-audit gate.

## Eval suite — GREEN (task #11)

- Full judged run (`.sandbox/eval-results/20260611-152348`): **20 PASS / 4 FAIL / 0 skip / 0 judge-err**.
  All 4 FAILs were diagnosed, evidenced against the original ADK sources (read-only
  `idc-test-repo`), ruled on by team-lead, and fixed:
  1. **3 happy-path evalsets mis-migrated** (`plan-admitted-consideration`,
     `subphase-polish-all-pillars`, `think-basic-consideration`): refusal codes sat in
     `binary.any_of` (required-present) with `expect_refusal:false` — unpassable by a
     correct happy answer. Original rubrics define `expected_refusals: []` +
     `forbidden_gate_violations` (must-be-absent). Fixed `any_of → []`,
     `none_of → ["outside_allowed_globs","forbidden_glob_hit"]` (commit `54beee8`).
  2. **Sequence refusal-code collision** (`sequence-missing-polished-pillar-ref`): the
     sandbox WORKFLOW.md's migration-authored definitions of `scope_invention_denied` and
     `missing_polished_pillar_reference` overlapped; the agent's "wrong" code was
     doctrine-correct. Definitions sharpened to be disjoint per the original `issue.md`
     briefs; prompts and evalsets stayed verbatim (commit `1477af2`). §6.6 guard-rail
     untouched.
- Rerun of all 8 affected sets (`--timeout 600`, judged): **11/11 PASS, exit 0** —
  4 fixed + 7 insurance re-passes, zero regressions (`.sandbox/eval-results/20260611-162901`).
- **Union: 24/24 cases PASS on latest run.**

## Runner hardening (task #13)

`run-evals.sh` now scores agent-side infra errors (usage-limit banner in place of an
answer, or empty agent output from timeout/crash) **ERR** via the judge-err path (exit 3),
never FAIL (commit `d428dc5`). Functionally verified: a forced 1-second-timeout case
scored `ERR det:ERR judge:agent-err`, suite exited 3. Docs synced.

## Stage 2 — Install + DoD

- Marketplace added from the local repo; `idc@idc-workflow` 0.1.0 installed user-scope and
  **disabled** (per-project scoping model).
- **DoD #1:** fresh sandbox → doctor pre-init 4/5 with the correct fix hint → `/idc:init`
  → doctor **5/5 PASS**. Found and fixed a real init bug en route (commit `2b5e85e`):
  Phase 2 treated `docs/workflow/` as one existence-checked unit, so a partial tree never
  got repaired — doctor's "run `/idc:init`" hint looped. Now per-entry copy-if-missing.
  No board provisioned: sandbox is filesystem-backend with no remote; init explained why
  and (correctly) refused to rewrite operator tracker config.
- **DoD #2:** non-IDC scratch dir showed zero `idc:`-prefixed plugin entries pre-deletion
  (loose originals still visible, as expected); post-Stage-4 the same check returns
  **"NONE"** — nothing IDC leaks into non-enabled projects.
- **DoD #3 (second-machine sim):** clean GitHub clone → marketplace add → install →
  headless session resolved all 8 `/idc:*` commands. Re-runs against the recreated remote
  before the gate.
- **DoD #4 (Codex chain):** `install-codex.sh` converted `~/.agents/skills` to a real
  dir; **5/5 adapters resolve** through the two-hop links; personal skills intact
  (verified before AND after Stage 4).

## Stage 3 — knowledge-engine cutover

- Pre-checks: repo clean, idle (no writes in the prior hour).
- **PR #348** (squash-merged per repo convention): all `/agent-*` slash refs → `/idc:*`;
  `~/.claude/agents|skills` IDC pointers → `idc:` namespaced names; Codex sibling path →
  `~/.agents/skills/...`; "live under ~/.claude" location statements → the plugin;
  `.claude/settings.json` enables `idc@idc-workflow`. Reference updates only.
- **Deliberately untouched (operator-visible follow-up, needs its own Ripple):** the
  governance-pipeline surface enumeration (WORKFLOW.md §4) and the delete-sweep rule
  still cite `~/.claude/agents/` / `~/.claude/skills/` as governance surfaces — that is
  doctrine, not a reference, and post-cutover it should likely point at the plugin repo.
  Also left: retired `idc-role-phase-tracker` historical mentions, branch/worktree naming
  conventions, `team-execute` personal agents.
- Spot-check: a headless knowledge-engine session resolves all 8 commands, the codex
  adapters, the `idc:idc-skill-*` set, and the `idc:idc-build` agent type. Read-only.

## Stage 4 — ~/.claude originals deleted

- Backups verified first: `~/Archives/idc-migration-backup-2026-06-11.tar.gz`
  (5933 entries, spot-checked) and the refreshed dev-history bundle (below).
- Deleted **105 paths** (manifest captured at the time of deletion): 23 agent bodies +
  runbook + `references/codex-name-aliases.md` (+ `.bak-*` snapshots), all 38 IDC skill
  dirs, 6 old `/agent-*` command files (+ `.bak`s). `~/.claude/skills` 75 → 37 entries.
- Codex mirror re-synced (`install-codex.sh` re-run: 5/5 adapters) and the 33 dangling
  links to deleted skills pruned manually (`~/.agents/skills` now 42 = 37 personal + 5
  adapters, 0 dangling). Confirms known-debt **MIN-14** (no automatic pruning) — the
  re-sync issue stays on the Stage 6 list.
- Post-deletion: scratch zero-entries check = "NONE"; sandbox doctor **5/5**.

## Stage 5 — scrub + history

- **Secret sweep:** high-signal patterns (AWS/GitHub/OpenAI/Slack/Google keys, private-key
  blocks) — zero hits tree-wide. `gitleaks` not installed; grep battery used.
- **Personal identifiers:** redacted from dev-log docs (commit `c016945`) — the audit
  report quoted the personal email verbatim as BLK-1 evidence; two briefs carried
  `/Users/<name>` paths. Tree-wide grep now zero.
- **CHANGELOG** converted to a real 0.1.0 entry (commit `f9d6de7`).
- **BLK-1 history rewrite:** fresh-orphan-root squash → `main` is a single
  noreply-authored commit (`2037595`, tree verified byte-identical to the final dev main)
  + follow-ups. Force-pushed; CI green.
- **Auditor BLOCKER (supersedes the force-push):** GitHub retains force-pushed objects
  server-side — old SHAs (`e206932` et al.) still resolve via API. **Delete+recreate of
  the GitHub repo is mandatory before the flip** (team-lead endorsed). Pending operator
  unblock: grant `delete_repo` scope or delete via UI; then phase-e recreates PRIVATE,
  pushes ONLY clean `main`, re-runs the clone DoD, and requests the audit gate (which
  404-probes the old SHAs).
- **Local hygiene done:** dev-history bundle refreshed (`--all`, verify-passed; contains
  `dev-history` @ `23e497d` + clean `main`) and the local `dev-history` branch deleted —
  no future `--all`/`--mirror` push can re-leak. The dev checkout now carries only clean
  refs.

## Pending at writing

1. **Operator:** delete `llamallamaredpajama/idc-workflow` on GitHub (or grant
   `delete_repo`) — then phase-e recreates + pushes clean main.
2. **fidelity-audit gate** (post-recreate): 404 probes + Lane-4 re-sweep + delta review →
   GO/NO-GO.
3. On GO: flip public → tag `v0.1.0` + GitHub release → CI green check → Stage 6 issues
   (known-debts register incl. dangling-skill refs, none_of population for remaining
   sets, MIN-14 mirror re-sync, init board-provisioning §6.x citation debts, the
   pi-idc-collab cross-runtime compatibility issue, knowledge-engine governance-surface
   Ripple follow-up).

## Residual risks / notes for the operator

- GitHub may cache pre-rewrite objects even after recreate only if the same repo is
  *renamed* rather than deleted; the delete+recreate path is deterministic. The audit
  gate verifies with API 404 probes.
- The eval suite's two heavyweight planning-chain cases need `--timeout 600`; the default
  360s can time them out (scored ERR, not FAIL, since `d428dc5`).
- Plugin updates after repo edits require `claude plugin uninstall && install` (the
  install cache does not track the repo working tree).
