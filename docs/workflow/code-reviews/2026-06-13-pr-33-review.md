# PR #33 Review — te-A3: Stage field (4→5) + pointer items

- **PR:** #33 · **Issue:** #26 (unit A3) · **Run:** pi-unification-plan-2026-06-13-0352
- **Head:** `c8c4220` (feat green) · **Red:** `f454cf8` (test) · **Base/merge-base:** `b5aee90`
- **Slice:** feature (TDD required) · **Layers claimed:** schema, service, cli
- **Reviewer:** pr-reviewer (adversarial, independent — writer self-report NOT trusted)

## VERDICT: ✅ APPROVE — merge-ready (2 Minor doc-staleness nits, non-blocking)

No Blocker, no Major. Every Done-When clause verified independently against the shipped
scripts in a detached scratch worktree (removed after). Two cosmetic 4→5 doc inconsistencies
the PR missed; neither affects behavior or any smoke test.

---

## TDD discipline — PASS

| Check | Result |
|---|---|
| Commit ordering | `test(A3): red` (`f454cf8`) **first**, `feat(A3): green` (`c8c4220`) second ✓ |
| Red commit is test-only | Yes — single file `tests/smoke/phase1-tracker-stage.sh` (+96), no impl ✓ |
| **Red was real** | At `f454cf8` the test exits **1** with `idc_tracker_fs.py: error: unrecognized arguments: --stage Consideration` → `FAIL: create pointer failed`. Feature genuinely absent — **NOT a syntax error** (`bash -n` clean). ✓ |
| Goalposts unmoved | `git diff f454cf8 c8c4220 -- tests/smoke/phase1-tracker-stage.sh` is **empty** — green did not touch the red test ✓ |
| `Closes #26` | Present in PR body ✓ |

## Substance — Done-When holds (independently reproduced)

**Stage query exclusion (filesystem helper, my own fixtures):** `query --stage Buildable`
returned **only** the buildable issue; both the `Consideration` and `Planning` pointers were
excluded. Invalid `--stage Frobnicate` rejected (exit 1) against exactly
`Consideration|Planning|Buildable`.

**Glass wall (`idc_autorun_drain.py`, independent run):** `eligible: 3 4` — buildable issue
included, legacy no-Stage issue included (additive), **both pointers (Consideration + Planning)
excluded**. `agents/idc-autorun.md` rewritten to query `Stage = Buildable` / `Stage =
Consideration` instead of scanning `docs/considerations/` — glass wall intact.

**Pointer-shape distinctness (`idc_schema_check.py`, independent fixtures):**
- valid pointer (`File:` + Stage/Phase/Domain, no contract) → PASS
- valid buildable 6-element contract → PASS
- pointer carrying `GOAL:`/`VERIFICATION SURFACE:` → **REJECTED** (duplicated-canonical-content guard) ✓
- pointer missing `File:` → REJECTED ✓
- unknown `Stage:` value → REJECTED ✓
- **legacy regression:** body with no `Stage:` line still runs the full contract check (valid→PASS, incomplete→FAIL) ✓

**Exactly 5 fields / no creep:** `FIELDS = ("Status","Wave","Phase","Domain","Stage")` (5),
`STAGES` = the 3 values, board table header renders `Status | Stage | Wave | Phase | Domain`.
**Additive** confirmed: empty Stage reads as buildable (eligible in drain, contract-checked in
schema) — legacy 4-field repos keep working.

**doctor.md untouched:** `commands/doctor.md` NOT in the diff; board validation delivered via
`scripts/idc_schema_check.py` per the card. ✓

## Layers — PASS
- **schema:** `idc_schema_check.py`, `skills/idc-schema-check`, `tracker-config.yaml`, `WORKFLOW.md §3.1/§3.2`
- **service:** `idc_tracker_fs.py`, `idc_autorun_drain.py`
- **cli:** `idc_tracker_fs.py` argparse (`--stage` on create/query), `commands/init.md` 5-field provisioning

## Deep-module / boundaries
- **Net-new authored files: 1** (the test) — well under the >4 Major threshold.
- **Off-limits CLEAN:** no `agents/idc-build.md` (A2), no A1 review files, no `commands/doctor.md`
  (B1), no Track B, no `no_edit_paths` (`docs/dev/2026-06-13-pi-unification-plan.md`, `LICENSE`).
- `bash scripts/lint-references.sh` → **CLEAN (30 files)**.
- `bash tests/smoke/run-all.sh` → **ALL GREEN (10/10)** incl. the new test and shared-script
  consumers (phase1-tracker-fs / phase3-plan / phase6-autorun).

## Findings

### Minor (2) — non-blocking 4→5 doc staleness
1. **`templates/tracker-config.yaml:4`** — header comment still reads "the four canonical
   field node IDs", contradicting the same file's lines 7/11/20 ("all five names", "exactly
   FIVE custom fields") and the now-5-entry `field_ids` block. Should say "five".
2. **`skills/idc-tracker-github/SKILL.md:117`** — "Board + **the four fields** are provisioned
   by `/idc:init`" contradicts this skill's own updated header ("exactly five custom fields")
   and `commands/init.md`, which now provisions 5. Should say "five".

Both are stand-alone one-word doc fixes; safe to land as-is or fold into a follow-up. (Note:
the *other* "4-field"/"legacy 4-field" mentions in `idc-tracker-github:28`,
`idc-tracker-filesystem:23`, `WORKFLOW.md:77`, and `init.md:92` ("four v2 *values*" = Status
options) are all **correct** legacy-migration / Status-value references, not stale.)

### Nit / Blocker / Major
None.
