# PR #31 review — te-B4: governance compiler

**VERDICT: APPROVE** (0 Blocker · 0 Major · 0 Minor · 1 Nit) — adversarially verified at PR head `ef49005` in an isolated detached scratch worktree.

- PR: #31 · Issue: #28 (unit B4) · run: pi-unification-plan-2026-06-13-0352
- Layers claimed: service, output · Slice: feature (TDD required)
- Reviewer made NO source edits. All checks reproduced independently — writer self-report not trusted.

---

## Done-When (issue #28) — MET
> `idc_governance_compile.py` emits a byte-stable sidecar across repeated runs on unchanged inputs, and `idc_governance_check.py` returns 0 on a matching sidecar but non-zero after WORKFLOW.md is mutated — both asserted by a new standalone smoke test.

Independently confirmed (see evidence below): byte-stable across **5** compiles, check 0-on-match / non-zero-on-mutation, asserted by `tests/smoke/phase8-governance.sh`.

## Special check — writer slip (file written into MAIN then removed) — CLEAN
- `git -C <main> status --short` shows only `?? docs/workflow/` (the code-review reports dir). **No stray B4 files** in the main checkout: no `scripts/idc_governance_*.py`, no `idc-governance-contract.yaml`.
- All B4 deliverables are committed **on the PR branch** (verified in scratch worktree):
  - `scripts/idc_governance_compile.py` (220 lines, added)
  - `scripts/idc_governance_check.py` (122 lines, added)
  - `tests/smoke/phase8-governance.sh` (89 lines, added)
  - `tests/smoke/run-all.sh` — one-line `phase8-governance` registry append (the known shared-append surface)

## TDD ordering — PASS · Red was real
- Commit order: `5614dbf test(B4): red` → `ef49005 feat(B4): green`. Correct.
- At red sha `5614dbf`, `scripts/idc_governance_compile.py` does **not exist in the tree** (`git show` → "exists on disk, but not in '5614dbf'").
- Ran the red-sha test against an absent compiler → exits **1** with `FAIL: compiler not found ... (not implemented yet)`.
- `bash -n` on the red test passes → failure was the **assertion**, not a syntax error. Genuine red.

## Determinism — PASS
5 independent compiles on byte-identical sources → all 5 sidecars byte-identical (`cmp -s`). Deterministic by construction: fixed key order, `SOURCES`/`GLASS_WALL` fixed tuples, `fields = sorted(...)` (verified emitted as `[Domain, Phase, Status, Wave]`), raw-byte `sha256` hashes, no timestamps/randomness/abs-paths. Stdlib-only (no PyYAML).

## Drift detection / fail-closed — PASS
| scenario | exit | expected |
|---|---|---|
| sidecar matches | 0 | 0 |
| WORKFLOW.md mutated | 1 | non-zero |
| restored to original bytes | 0 | 0 (proves **hash-based**, not mtime) |
| tracker-config.yaml mutated | 1 | non-zero (all 3 sources watched) |
| sidecar missing | 2 | non-zero (fail-closed) |
| sidecar bad-hash / wrong schema_version | 2 | non-zero (malformed → fail-closed) |

- **Never auto-compiles**: after a missing-sidecar check, no sidecar was created. Confirmed both by behavior and by code (`cmd_check` only re-hashes + compares; no write path).
- **Pi-mode only / does NOT gate Claude/Codex**: no command/agent/skill invokes either script (grep). Only consumers are the smoke test + the scripts' own docstrings + the plan-doc spec. Nothing wires it into a Claude/Codex gate.

## Side-effect cleanliness — PASS
`bash tests/smoke/run-all.sh` → **10/10 green** (incl. `phase8-governance`), exit 0. `git status --short` **clean** afterward — the smoke sandboxes everything in `$(mktemp -d)` with a cleanup trap, including the default-out emit path. No stray `docs/workflow/idc-governance-contract.yaml` left in the repo.

## Project protocol — PASS
- `commands/doctor.md` **untouched** (B1-owned) — drift delivered via the check script; the "doctor reports staleness" line is correctly left as an operator follow-up.
- no_edit_paths (`docs/dev/2026-06-13-pi-unification-plan.md`, `LICENSE`) **not touched**.
- `bash scripts/lint-references.sh` → CLEAN (30 files scanned).
- Deep-module: 3 net-new authored files (2 scripts + 1 smoke) ≤ 4. PASS.
- Layers (service = the two scripts; output = emitted sidecar) map to changed paths.

---

## Findings

### Nit (non-blocking, defensible as-is)
- **`glass_wall` summary is a hardcoded schema-v2 invariant, not parsed from WORKFLOW.md.** `GLASS_WALL` is a fixed constant emitted verbatim, so if a repo edited its glass-wall prose, the WORKFLOW.md hash would change (→ drift fires, correct) but the regenerated summary scalar would be unchanged. This is acceptable: the glass wall is an IDC v2 invariant (documented in-code citing WORKFLOW.md §1.2), the sha256 hashes are the safety-critical core, and the summary is explicitly labeled convenience/best-effort. No action required; noted for the convergence consumer (B2/launcher) so it relies on `source_hashes`, not the summary scalars, for trust.

## Verdict
**APPROVE.** Done-When met and independently reproduced; TDD red genuine; determinism, drift, and fail-closed all verified; main checkout clean of the self-reported slip; protected paths untouched; full smoke + lint green. The one Nit is by-design and does not affect correctness or the verdict.
